# **基于Web的宠物领养信息发布与匹配系统**

# **项目指导文档（AI实现版）**

------

## **1. 项目概述**

### **1.1 项目背景**

随着城市化进程加快，流浪动物数量逐年攀升，传统领养依赖线下救助站宣传，存在信息传播范围窄、匹配效率低等问题。同时，现代人群对宠物陪伴需求增长，但缺乏便捷的领养信息获取渠道，导致“领养难”与“安置难”矛盾突出。

### **1.2 项目目标**

开发一个基于Web的宠物领养信息发布与匹配系统，整合救助机构与领养者资源，实现：

- **宠物信息规范化发布**
- **领养需求精准匹配**
- **信息透明化减少领养纠纷**
- **提升流浪动物安置效率**

### **1.3 技术栈**

- **后端框架**：Python + Django (推荐) 或 Node.js + Express
- **前端框架**：Vue.js 或 React
- **数据库**：MySQL 或 PostgreSQL
- **服务器**：阿里云 Ubuntu 20.04/22.04 LTS
- **部署环境**：Nginx + Gunicorn (Django) / PM2 (Node.js)
- **AI匹配模块**：可基于规则引擎或简单推荐算法

------

## **2. 系统架构**

text

```
用户层 (Web浏览器)
    |
应用层 (Nginx反向代理)
    |
后端服务 (Django/Node.js + Gunicorn/PM2)
    |
数据层 (MySQL/PostgreSQL)
    |
静态资源 (CSS/JS/图片上传目录)
```



------

## **3. 功能需求详细说明**

### **3.1 用户管理模块**

- **用户注册/登录**：手机号/邮箱注册，JWT身份验证
- **角色区分**：
  - **领养者**：浏览宠物、提交申请、收藏宠物
  - **救助机构**：发布宠物信息、审核申请、管理已发布信息
  - **管理员**：用户管理、内容审核、数据统计

### **3.2 宠物信息发布模块**

- **信息字段**：
  - 宠物基本信息：名称、种类（猫/狗等）、品种、年龄、性别、绝育情况
  - 健康信息：疫苗接种、病史、驱虫情况
  - 领养要求：居住环境、经济能力、经验要求等
  - 图片上传：支持多图上传，自动压缩
  - 地理位置：关联救助机构地址

### **3.3 匹配推荐模块**

- **领养者偏好设置**：
  - 宠物类型偏好
  - 年龄范围偏好
  - 地理位置半径（如：20公里内）
- **匹配算法**：
  - 基于规则的匹配（标签匹配度计算）
  - 可扩展为协同过滤推荐（后续升级）

### **3.4 领养申请流程**

1. 领养者浏览宠物详情页
2. 提交领养申请表（填写家庭情况、养宠经验等）
3. 救助机构后台审核申请
4. 状态更新：待审核 → 初审通过 → 家访安排 → 领养完成
5. 站内消息通知

### **3.5 后台管理模块**

- 用户管理（启用/禁用账户）
- 宠物信息审核（防止虚假信息）
- 领养申请跟踪
- 数据统计面板：领养成功率、热门宠物类型等

------

## **4. 部署实施步骤（阿里云Ubuntu）**

### **4.1 环境准备**

bash

```
# 1. 更新系统
sudo apt update && sudo apt upgrade -y

# 2. 安装Python环境（如选择Django）
sudo apt install python3-pip python3-dev python3-venv nginx curl -y

# 3. 安装数据库（以MySQL为例）
sudo apt install mysql-server libmysqlclient-dev -y
sudo mysql_secure_installation

# 4. 安装Node.js（如选择Node.js后端）
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install nodejs -y
```



### **4.2 数据库配置**

sql

```
-- 创建数据库和用户
CREATE DATABASE pet_adoption DEFAULT CHARACTER SET utf8mb4;
CREATE USER 'pet_user'@'localhost' IDENTIFIED BY 'StrongPassword123!';
GRANT ALL PRIVILEGES ON pet_adoption.* TO 'pet_user'@'localhost';
FLUSH PRIVILEGES;
```



### **4.3 项目部署结构**

text

```
/home/ubuntu/pet-adoption-system/
├── backend/          # 后端代码
├── frontend/         # 前端代码
├── static/           # 静态文件
├── media/            # 用户上传文件
├── venv/             # Python虚拟环境
└── .env              # 环境变量
```



### **4.4 后端部署示例（Django）**

bash

```
# 1. 创建项目目录
mkdir -p /home/ubuntu/pet-adoption-system
cd /home/ubuntu/pet-adoption-system

# 2. 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 3. 安装依赖
pip install django gunicorn mysqlclient pillow django-cors-headers

# 4. 初始化项目（AI需生成实际代码）
django-admin startproject backend
cd backend

# 5. 配置文件示例（settings.py片段）
"""
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'pet_adoption',
        'USER': 'pet_user',
        'PASSWORD': 'StrongPassword123!',
        'HOST': 'localhost',
        'PORT': '3306',
    }
}
"""

# 6. 迁移数据库
python manage.py makemigrations
python manage.py migrate
python manage.py createsuperuser

# 7. 收集静态文件
python manage.py collectstatic
```



### **4.5 Nginx配置**

nginx

```
# /etc/nginx/sites-available/pet-adoption
server {
    listen 80;
    server_name your_domain_or_ip;
    
    location /static/ {
        alias /home/ubuntu/pet-adoption-system/static/;
    }
    
    location /media/ {
        alias /home/ubuntu/pet-adoption-system/media/;
    }
    
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```



### **4.6 Gunicorn服务配置**

bash

```
# /etc/systemd/system/gunicorn.service
[Unit]
Description=Gunicorn for Pet Adoption System
After=network.target

[Service]
User=ubuntu
Group=www-data
WorkingDirectory=/home/ubuntu/pet-adoption-system/backend
ExecStart=/home/ubuntu/pet-adoption-system/venv/bin/gunicorn \
    --workers 3 \
    --bind unix:/home/ubuntu/pet-adoption-system/backend.sock \
    backend.wsgi:application

[Install]
WantedBy=multi-user.target
```



### **4.7 启动服务**

bash

```
# 1. 启用Nginx配置
sudo ln -s /etc/nginx/sites-available/pet-adoption /etc/nginx/sites-enabled
sudo nginx -t
sudo systemctl restart nginx

# 2. 启动Gunicorn
sudo systemctl start gunicorn
sudo systemctl enable gunicorn

# 3. 开放防火墙
sudo ufw allow 'Nginx Full'
sudo ufw allow ssh
sudo ufw enable
```



------

## **5. AI实现指导说明**

### **5.1 代码生成提示**

- **模块化开发**：按功能模块分阶段生成代码
- **API优先**：先实现RESTful API接口，再开发前端界面
- **错误处理**：每个API必须包含适当的异常处理
- **安全考虑**：
  - SQL注入防护（使用ORM）
  - XSS防护（模板自动转义）
  - CSRF保护
  - 文件上传类型限制

### **5.2 匹配算法实现示例**

python

```
# 简化版匹配算法
def calculate_match_score(pet, user_preferences):
    score = 0
    if pet.pet_type in user_preferences['preferred_types']:
        score += 30
    if user_preferences['max_distance'] >= calculate_distance(
        pet.location, user_preferences['location']
    ):
        score += 25
    if pet.age >= user_preferences['min_age'] and \
       pet.age <= user_preferences['max_age']:
        score += 20
    # ... 其他匹配规则
    return score
```



### **5.3 数据库模型示例**

python

```
# Django模型示例
class Pet(models.Model):
    PET_TYPES = [('dog', 'Dog'), ('cat', 'Cat'), ('other', 'Other')]
    name = models.CharField(max_length=100)
    pet_type = models.CharField(max_length=20, choices=PET_TYPES)
    breed = models.CharField(max_length=100, blank=True)
    age = models.IntegerField()  # 以月为单位
    # ... 其他字段
```



------

## **6. 测试与验证**

### **6.1 功能测试清单**

- 用户注册登录流程
- 宠物信息发布（带图片上传）
- 搜索过滤功能
- 匹配推荐展示
- 领养申请提交
- 后台审核流程
- 响应式前端布局

### **6.2 性能要求**

- 页面加载时间 < 3秒
- 支持100+并发用户
- 图片懒加载
- 数据库查询优化（索引）

------

## **7. 项目时间线建议**

1. **第1周**：环境搭建 + 基础架构
2. **第2周**：用户系统 + 权限管理
3. **第3周**：宠物信息CRUD + 图片上传
4. **第4周**：匹配算法 + 搜索功能
5. **第5周**：领养申请流程
6. **第6周**：后台管理系统
7. **第7周**：部署优化 + 测试
8. **第8周**：文档完善 + 演示准备

------

## **8. 故障排查指南**

bash

```
# 查看服务状态
sudo systemctl status gunicorn
sudo systemctl status nginx

# 查看日志
sudo journalctl -u gunicorn -f
sudo tail -f /var/log/nginx/error.log

# 数据库连接测试
mysql -u pet_user -p -e "USE pet_adoption; SHOW TABLES;"
```



------

## **9. 扩展建议（未来升级）**

1. **移动端APP**：React Native或Flutter开发
2. **智能匹配**：集成机器学习推荐算法
3. **在线咨询**：集成即时通讯功能
4. **领养后跟踪**：定期回访问卷系统
5. **数据分析平台**：流浪动物趋势分析


**适用对象**：AI代码生成工具 + 开发者
**部署环境**：阿里云Ubuntu 20.04/22.04 LTS

> **提示AI**：请按照模块分阶段生成代码，每个阶段完成后提供测试验证步骤。优先确保核心功能（用户管理+宠物发布+基础匹配）可用，再逐步添加高级功能。